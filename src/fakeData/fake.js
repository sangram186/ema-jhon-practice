

const data = { name: 'Laptop', processor: 'intel i9'};

const data2 = { "name": "Laptop", "processor": "intel i9"};

const [count, setCount] = useState([]);

const [count2, steCount2] = useState(0 or anything);

const {img, name, seller, stock, price} = product;

const [count, setCount] = dataCount;

